pytest>=8.0

# The explanation helpers describe their schema with pydantic and import it at
# module level. Nothing here needs the Anthropic SDK: the client is built inside
# the function that calls it, so these tests never reach for a key.
pydantic>=2.0

# The three scripts that touch a collection import the Anki library at module
# level, so the helpers inside them cannot be imported without it — even the
# ones that are pure string handling. It is an 11MB wheel and it means these
# tests actually run rather than skipping quietly in CI.
anki>=25.0
